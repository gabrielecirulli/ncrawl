// Generated by CoffeeScript 1.3.3
(function() {
  var net, util, _;

  util = require('./util');

  net = require('net');

  _ = require('underscore');

  module.exports = function(raw) {
    var finish, push, split, start, target, targets, _i, _len, _ref;
    if (raw == null) {
      raw = '';
    }
    targets = [];
    push = function(target) {
      target = target.trim();
      if (target) {
        return targets.push(target);
      }
    };
    _ref = raw.split(',');
    for (_i = 0, _len = _ref.length; _i < _len; _i++) {
      target = _ref[_i];
      split = target.split('-');
      if (split.length === 2 && net.isIPv4(split[0]) && net.isIPv4(split[1])) {
        start = util.IPv4ToLong(split[0]);
        finish = util.IPv4ToLong(split[1]);
        while (!(start > finish)) {
          push(util.longToIPv4(start++));
        }
      } else {
        push(target);
      }
    }
    return _.uniq(targets);
  };

}).call(this);
